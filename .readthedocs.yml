version: 2

sphinx:
  configuration: docs/conf.py

# Build all formats

formats: all

conda:
  environment: docs/environment.yml

build:
  os: ubuntu-22.04
  tools:
    python: mambaforge-4.10
  jobs:
    pre_build:
      - echo $PATH
      - cd docs/diagrams && make
      - echo $DENO_DOM
      - echo $DENO_DOM_VERSION
      - echo $DENO_INSTALL_ROOT
      - echo $QUARTO_DENO
      - echo $QUARTO_DENO_DOM
      - cd tutorial && make

